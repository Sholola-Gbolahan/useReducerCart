import React from "react"
import {
  CLEAR_CART,
  REMOVE_ITEM,
  INCREASE_AMOUNT,
  DECREASE_AMOUNT,
} from "./actions"

const reducer = (state, action) => {
  return <div>reducer</div>
}

export default reducer
